docker run -d \
  --name postgres \
  -e POSTGRES_PASSWORD=twf65132090TWF \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_DB=postgres \
  -p 5432:5432 \
  -v /data/postgres/data:/var/lib/postgresql/data \
  --restart always \
  postgres:16


docker run -d --name neo4j \
    --publish=7474:7474 --publish=7687:7687 \
    --volume=/data/neo4j/data:/data \
    -e NEO4J_PLUGINS=["apoc"] \
    -e NEO4J_AUTH=neo4j/twf65132090TWF \
    neo4j

密码版：
docker run -d --name redis -p 6379:6379 -v /data/redis/data:/data redis redis-server --requirepass twf65132090TWF --appendonly yes

无密码版
docker run -d --name redis -p 6379:6379 -v /www/redis/data:/data redis redis-server --appendonly yes

milvus：

curl -sfL https://raw.githubusercontent.com/milvus-io/milvus/master/scripts/standalone_embed.sh -o standalone_embed.sh

bash standalone_embed.sh start


